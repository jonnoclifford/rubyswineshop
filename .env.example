# Site Configuration
NEXT_PUBLIC_SITE_URL=https://rubyswineshop.com.au

# Google Maps API
# Get your API key from: https://console.cloud.google.com/google/maps-apis
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_api_key_here

# Tina CMS Configuration
# Required for the CMS to function properly
#
# Setup Instructions:
# 1. Go to https://app.tina.io and sign up for a free account
# 2. Create a new project or select an existing one
# 3. Navigate to "Configuration" or "Overview" in your project dashboard
# 4. Copy your Client ID and paste it below (starts with a random string)
# 5. Generate a Read-Only token or Content token and paste it below
# 6. Set the branch to match your git branch (usually "main" or "master")
#
# Note: NEXT_PUBLIC_* variables are exposed to the browser
# TINA_TOKEN should remain server-side only
NEXT_PUBLIC_TINA_CLIENT_ID=your_tina_client_id_here
TINA_TOKEN=your_tina_read_only_token_here
NEXT_PUBLIC_TINA_BRANCH=main

# Set to true for local development (uses filesystem instead of Tina Cloud)
TINA_PUBLIC_IS_LOCAL=true
