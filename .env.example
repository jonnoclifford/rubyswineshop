# Site Configuration
NEXT_PUBLIC_SITE_URL=https://rubyswineshop.com.au

# Google Maps API
# Get your API key from: https://console.cloud.google.com/google/maps-apis
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_api_key_here

# Tina CMS Configuration
# Required for the CMS to function properly
#
# Setup Instructions:
# 1. Go to https://app.tina.io and sign up for a free account
# 2. Create a new project or select an existing one
# 3. Navigate to "Configuration" or "Overview" in your project dashboard
# 4. Copy your Client ID and paste it below (starts with a random string)
# 5. Generate a Read-Only token or Content token and paste it below
# 6. Set the branch to match your git branch (usually "main" or "master")
#
# Note: NEXT_PUBLIC_* variables are exposed to the browser
# TINA_TOKEN should remain server-side only
NEXT_PUBLIC_TINA_CLIENT_ID=your_tina_client_id_here
TINA_TOKEN=your_tina_read_only_token_here
NEXT_PUBLIC_TINA_BRANCH=main

# Set to true for local development (uses filesystem instead of Tina Cloud)
TINA_PUBLIC_IS_LOCAL=true

# GitHub OAuth Authentication
# Required for secure admin panel access
#
# Setup Instructions:
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App" (or "New GitHub App" for advanced features)
# 3. Fill in the application details:
#    - Application name: Ruby's Wine Bar Admin
#    - Homepage URL: https://rubyswineshop.com.au
#    - Authorization callback URL: https://rubyswineshop.com.au/api/auth/github/callback
#      (For local dev: http://localhost:3000/api/auth/github/callback)
# 4. Click "Register application"
# 5. Copy the "Client ID" and paste it below
# 6. Click "Generate a new client secret" and copy it immediately
# 7. Paste both values below
# 8. Add comma-separated list of authorized GitHub usernames
#
# Example: GITHUB_AUTHORIZED_USERS=octocat,johndoe,janedoe
#
# Note: These credentials authenticate users, not API access
GITHUB_CLIENT_ID=your_github_oauth_client_id_here
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret_here
GITHUB_CALLBACK_URL=https://rubyswineshop.com.au/api/auth/github/callback
GITHUB_AUTHORIZED_USERS=jonnoclifford

# GitHub API Configuration
# Required for reading and writing site-config.json from/to GitHub
#
# Setup Instructions:
# 1. Go to https://github.com/settings/tokens/new
# 2. Create a "Personal access token (classic)"
# 3. Give it a descriptive name (e.g., "Ruby's Wine Shop CMS")
# 4. Set expiration (recommended: 90 days or custom)
# 5. Select scopes: "repo" (Full control of private repositories)
# 6. Click "Generate token" and copy it immediately (you won't see it again)
# 7. Paste the token below as GITHUB_TOKEN
#
# Repository Configuration:
# - GITHUB_OWNER: Your GitHub username or organization
# - GITHUB_REPO: Your repository name
# - GITHUB_BRANCH: The branch to read/write from (usually "main")
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_OWNER=jonnoclifford
GITHUB_REPO=rubyswineshop
GITHUB_BRANCH=main
